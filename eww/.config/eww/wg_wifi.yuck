(defwidget wg_wifi []
  (box 
    :orientation "v"
    :class "base"
    ; :style "padding:20px;"
    (box
      :style "padding:10px;"
      (_button :icon "󰤨 WIFI" :action "eww close control && kitty -e nmtui")
      ; (box "-")
    )
    (box
      :style "padding:10px;"
      (_button :icon "󰤨 BT" :action "eww close control && blueman-manager")
      ; (box "-")
    )
  )
)
