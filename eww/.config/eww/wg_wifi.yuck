(defwidget wg_wifi []
  (box 
    :orientation "v"
    :class "base"
    ; :style "padding:20px;"
    (box
      :style "padding:10px;"
      (_button :icon "󰤨 " :text "WIFI" :action "eww close control && kitty -e nmtui")
      ; (box "-")
    )
    (box
      :style "padding:10px;"
      (_button :icon "󰤨 " :text "BT" :action "eww close control && blueman-manager")
      ; (_button :icon "󰤨 " :text "BT" :action "eww close control && exec scripts/rofi-bluetooth")
      ; (box "-")
    )
  )
)
